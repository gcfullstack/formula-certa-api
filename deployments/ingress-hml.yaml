apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: api-example-hml
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: 100m
  labels:
    run: api-example-hml
spec:
  rules:
  - host: hml-api-example.gruposfa.com.br
    http:
      paths:
      - backend:
          serviceName: api-example-hml
          servicePort: 80
